# Database Configuration
# Primary: MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=llaqta_gold
DB_PORT=3306

# Fallback: SQLite (Always active as backup)
SQLITE_DB_PATH=database.sqlite

# Database Sandbox Configuration (for isolated testing)
DB_SANDBOX_HOST=external-mysql-sandbox.example.com
DB_SANDBOX_USER=sandbox_user
DB_SANDBOX_PASSWORD=
DB_SANDBOX_NAME=llaqta_gold_sandbox
DB_SANDBOX_PORT=3306
